# Local development server port
PORT=3000

# Mailchimp credentials
MAILCHIMP_API_KEY=your-mailchimp-api-key
MAILCHIMP_SERVER_PREFIX=usX
MAILCHIMP_LIST_ID=your-audience-id

# Front-end origins allowed to call the serverless API (comma separated)
ALLOWED_ORIGINS=https://lateefcobb.com,https://www.lateefcobb.com

# Optional webhook (Zapier/Slack/etc.) to receive each inquiry in parallel
INQUIRY_WEBHOOK_URL=
